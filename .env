# ─── Database (local Docker Postgres — see docker-compose.yml) ────────────────
DATABASE_URL="postgresql://aifos:aifos_local_dev@localhost:5432/aifos?schema=public"
# DATABASE_URL="file:./dev.db"

# Generated locally; replace in production. Used to encrypt session JWTs.
AUTH_SECRET="ZBm8YRCtSmokzzGSh/gAO/CluUnaTK4YjH4zB5ToGeY="
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Google OAuth — create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# ─── Redis (local Docker — see docker-compose.yml) ────────────────────────────
REDIS_URL="redis://localhost:6379"

# ─── Anthropic (Claude) — https://console.anthropic.com/settings/keys ─────────
ANTHROPIC_API_KEY=""
# Model used by the LangChain orchestrator + agents.
ANTHROPIC_MODEL="claude-opus-4-8"
